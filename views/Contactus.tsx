import React from "react";
import { BackCard } from "../components/BackCard";

type Props = {};

const Contactus = (props: Props) => {
  return <BackCard>Contactus</BackCard>;
};

export default Contactus;
